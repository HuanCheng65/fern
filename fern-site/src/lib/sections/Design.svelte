<script>
  /*
   * 设计这一章。
   *
   * 它不是一屏，是连续的五幕——设计在 Fern 里的权重配得上一整章，而每个设计决定
   * 都该有自己的画面和很少的一点文字：
   *
   *   01 新意舒展     一块真屏幕钉住、亮起，界面各部分从四面八方汇回原位
   *   02 内容参与视觉  同一块屏幕不松开，四个真实实例一个接一个换过去
   *   03 岛           界面退场只剩那座岛，它被拎出窗口，别的岛从四周汇来
   *   04 信息架构     五件真东西摊在纸上，正常滚动，各自进视口时起身
   *   05 收回来       收进另一个窗口，安静下来，只剩一句话
   *
   * 前三幕在同一块钉住的屏幕上演，所以它们是同一个组件（`Stage`）：中间不能松手，
   * 松一次手，「还是刚才那块屏」这件事就没了。
   *
   * 节奏是一路松下来的：前三幕一条不能断的线，第四幕正常滚动，第五幕再钉一次把它
   * 合上。收和拆用同一套度量、同一条顶栏，两头才看得出是同一台机器。
   *
   * 全章的比例是刻意的：**视觉占比全站最大，文字反而最少**。每一幕只有一句标题。
   * 「设计很重要」不写出来，让人滚过去之后自己感觉到。
   *
   * 一条边界：网页可以有舒展、解构、重组这样的叙事动效，但那是官网自己的展示方式
   * ——不去演示 Fern 里并不存在的转场。
   */
  import Title from './design/Title.svelte';
  import Stage from './design/Stage.svelte';
  import Order from './design/Order.svelte';
  import Close from './design/Close.svelte';
</script>

<Title />
<Stage />
<Order />
<Close />
