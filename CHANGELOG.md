# 更新日志

每个版本一节，标题就是版本号。发版时 `.github/build-manifest.py` 会把对应那一节
原样取出来，写进更新清单的 `notes`，用户在设置的「关于」里看到的就是这段文字。

所以这里写的是**给用户看的话**：说清楚这个版本改了什么、他会注意到什么变化。
实现上的取舍写在提交信息里，不写在这里。

未发布的改动记在 `## 未发布` 下面，发版时把标题改成版本号即可。

## 未发布

- 新增自动更新。Fern 会在启动后检查新版本，并可以直接下载安装。
- 新增更新通道。测试版可更早获得新功能，可在「设置 → 关于」中切换。

## 0.1.1-beta.2

- 首个通过发布流水线构建的版本。
