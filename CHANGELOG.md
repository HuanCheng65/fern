# 更新日志

每个版本一节，标题是版本号和发布日期。发布时 `.github/build-manifest.py` 会把对应
那一节原样取出来，写进更新清单的 `notes`——用户在设置的「关于」里读到的就是这段文字。

所以这里写的是**给用户看的话**：这个版本改了什么、他会注意到什么变化。实现上的
取舍写在提交信息里，不写在这里。写法规范见 AGENTS.md 的「更新日志」一节。

不用手写这一份。改动值得让用户知道时，在提交信息里加一条 `Release-Note:` 尾注；
`.github/draft-changelog.py` 把这些尾注汇进「未发布」小节（**汇完要读一遍**），
`.github/release.py` 在发版时把它定稿成一个版本小节。

## 未发布

## 0.1.1-beta.5 - 2026-08-12

### 新增

- 可以直接导入 Prism / MultiMC 的实例，游戏文件留在原处
- 添加现有游戏时会自动识别 Prism / MultiMC 的实例目录
- 新建实例改为按代折叠的版本选择，常用版本一步就能选中
- 1.12.2 及更早的版本可以安装 LiteLoader，并与 Forge 同时使用
- 快照以触发它的事件命名，如「安装 Create 之前」
- 点开快照先看到世界与模组清单，恢复移到下一步
- 快照会说明拍摄后有何变化，点世界名可直接恢复该世界
- 恢复世界默认另存为新存档，不再覆盖原存档
- 导出时可勾选带哪些世界与内容，整合包的配置可不带
- 设置新增存储页：查看各处占用，可清理缓存日志并瘦身共享文件
- 数据页合并为一张占用总表，并可把数据目录迁移到其他位置
- 数据目录与游戏目录也可以直接打开

### 改进

- 启动时并行下载游戏文件与准备 Java，启动前等待更短
- 启动进度如实反映当前阶段，已下载大小不再超过总量
- 快照按保留策略自动清理，删除实例时一并清除
- 从诊断卡片恢复模组前会先说明后果，确认后才执行
- 新建实例的版本列表补全了全部快照，并加上了展开动效
- 返回改为逐级退回，不再多退一级或回到刚离开的页面

### 修复

- 修复 1.13 至 1.18.2 的游戏无法启动的问题
- 修复 1.12.2 及更早的 Forge 实例无法启动的问题
- 修复 1.16.3 至 1.16.5 的 Forge 实例无法启动的问题
- 修复导入的外部实例因缺少原版版本描述而无法启动的问题
- 添加外部目录时会记下它的真实名称，OptiFine 等版本不再启动失败或存档错位
- 旧整合包遗留的失效 JVM 参数会被自动跳过，不再导致启动失败
- 修复自动选择的 Java 可能不符合版本要求的问题
- 修复 26.1 起的新版本号无法列出 NeoForge 版本的问题
- 修复添加现有游戏页面顶栏缺少标题和返回键的问题
- 设置页各行宽度一致，内存滑杆不再被挤成一小段
- 存储占用展开后各项不再叠在一起
- 存储占用条与色点的各部分更容易分辨
- 自动检查更新单独成行，并写明关闭后不会发出请求

## 0.1.1-beta.4 - 2026-08-11

### 新增

- 启动预检查会指出相互冲突的模组组合
- 首次启动向导中可直接完成正版登录，也可切换到外置登录或离线模式
- 在浏览器中完成正版登录后，可立即检查登录状态
- 添加账户时在菜单中直接选择登录方式

### 修复

- 各处的删除确认按钮使用一致的配色
- 下拉菜单不再使用系统样式，在深色界面下与其余部分一致
- 设置按钮上的新版本提示点不再被放大
- 状态岛的文字不再被截断或显示为等宽字体
- 同时支持多个加载器的模组不再被启动预检查误判为不适用
- 启动预检查不再将模组声明的冲突关系误读为缺少依赖
- 模组声明多个支持版本区间时，启动预检查不再误判为不适配
- 首次启动向导中勾选的现有版本会被正确添加为实例
- 首次启动向导的最后一步不再要求立即创建实例
- 正版登录过程中的短暂网络故障不再中断登录
- 正版登录时自动打开微软验证页面

## 0.1.1-beta.3 - 2026-08-09

### 新增

- 命令面板的账户与实例列表末尾可直接新建
- 账户头像改为显示实际皮肤，未设置时使用 Steve 或 Alex
- 启动页顶部显示本次启动使用的账户，点击可切换
- 设置中的 Java 按大版本列出，可查看安装位置与占用
- 内存改为标尺显示，标出自动分配值、上次峰值与已用内存

### 修复

- 自动选择的 Java 会满足模组声明的版本要求
- 快照版本的实例不再把所有模组都判为不兼容
- 强调色恢复跟随背景变化
- 实例不再自动绑定首次启动时使用的账户

## 0.1.1-beta.2

- 首个通过发布流水线构建的版本
