/**
 * 实例场景向内那一级里，不是实例的那几个。
 *
 * 纵深位平时装的是一个实例 id，但有两个是页面：新建、添加现有游戏。顶栏要拿
 * 它们的名字画面包屑，场景要拿它们的 id 决定渲染谁，命令面板要拿 id 跳过去。
 * 这三处如果各写各的字面量，迟早分叉——上一版就是这样：App 只认得 `new`，
 * 于是进了「添加现有游戏」之后顶栏既没有标题也没有返回键（顶栏靠标题非空
 * 才认定自己在纵深里）。
 *
 * 单独放一个文件而不是挂在场景上：场景、App、新建页三边都要用，挂在任何一边
 * 都会绕出一个循环 import。
 *
 * 实例 id 是随机发的，撞不上这两个词。
 */

export const CREATE = 'new'
export const EXISTING = 'existing'

/** id → 顶栏上的那个词。就是各自页面自己的标题，两处必须是同一个字符串。 */
export const DEPTHS: Record<string, string> = {
  [CREATE]: '新建实例',
  [EXISTING]: '添加现有游戏',
}
